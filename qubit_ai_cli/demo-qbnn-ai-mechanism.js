#!/usr/bin/env node

/**
 * QBNN Interactive Chat Demo
 * Topic: How AI Works (AIの仕組み)
 *
 * Demonstrates QBNN's approach to explaining AI mechanisms
 */

const colors = {
  reset: "\x1b[0m",
  bright: "\x1b[1m",
  dim: "\x1b[2m",
  cyan: "\x1b[36m",
  green: "\x1b[32m",
  yellow: "\x1b[33m",
  blue: "\x1b[34m",
  magenta: "\x1b[35m",
  red: "\x1b[31m",
};

function log(message, color = colors.reset) {
  console.log(`${color}${message}${colors.reset}`);
}

function section(title) {
  log("\n" + "═".repeat(75), colors.bright);
  log(`║ ${title.padEnd(73)} ║`, colors.bright);
  log("═".repeat(75) + "\n", colors.bright);
}

async function sleep(ms) {
  return new Promise((r) => setTimeout(r, ms));
}

// ============================================================================
// QBNN Chat Session Simulation
// ============================================================================

async function qbnnChatSession() {
  section("🤖 QBNN Interactive Chat - AIの仕組みについて");

  log("ユーザー質問:\n", colors.cyan);
  log("「AIの仕組みについて詳しく説明してください」\n", colors.bright);

  await sleep(1000);

  log("═".repeat(75), colors.dim);
  log("🧠 QBNN Processing...\n", colors.magenta);

  await sleep(1200);

  log("💬 QBNN Response:\n", colors.green);

  log(`AIの仕組みは、大きく分けて以下の3つのレベルで理解できます：

【レベル1】ニューラルネットワークの基本構造

AIの最も基本的な単位は「ニューロン」です。これは生物の脳細胞にインスパイア
されています。ニューロンは以下の要素で構成されます：

  • 入力（Input）: 複数の情報を受け取る
  • 重み（Weight）: 各入力の重要度を示す係数
  • バイアス（Bias）: 基本的な傾向を示す値
  • 活性化関数（Activation）: 出力を非線形に変換

数式で表すと：
  output = σ(Σ(weight_i × input_i) + bias)

ここで σ（シグマ）は活性化関数で、ReLU、Sigmoid、Tanhなどがあります。

【レベル2】層状構造による情報処理

複数のニューロンを組み合わせて「層」を作ります：

  入力層（Input Layer）
      ↓
  隠れ層1（Hidden Layer 1） - 特徴抽出
      ↓
  隠れ層2（Hidden Layer 2） - 特徴の組み合わせ
      ↓
  隠れ層3（Hidden Layer 3） - 高度な特徴
      ↓
  出力層（Output Layer） - 最終判断

例えば、画像認識では：
  • 入力層: ピクセル値（画像データ）
  • 隠れ層: エッジ、形、テクスチャなどの特徴
  • 出力層: 「猫」「犬」などの分類

【レベル3】学習メカニズム - 誤差逆伝播

AIが「学習」する仕組みは次のようなサイクルです：

  1. フォワードパス（Forward Pass）
     入力データを順方向に通す
     prediction = network(input)

  2. 損失計算（Loss Calculation）
     予測と正解のズレを計算
     loss = (prediction - target)²

  3. バックプロパゲーション（Backpropagation）
     誤差を逆方向に伝播
     各重みに対する勾配を計算

  4. 重みの更新（Weight Update）
     勾配降下法で重みを調整
     weight_new = weight_old - learning_rate × gradient

これを何万回も繰り返すことで、モデルは段々と精度を上げていきます。

【レベル4】QBNN的な観点 - 双方向処理

QBNN（量子的インスピレーションを受けたニューラルネットワーク）では、
さらに進化した処理を実現します：

  Forward Direction（前方向）:
    情報が入力から出力へ流れる

  Backward Direction（後方向）:
    検証と最適化が逆方向で同時に進行

  Bidirectional Optimization:
    前後の処理が相互に影響し合う
    より高速で精度の高い収束

【実践的な例】

入力: 「猫の写真」

フェーズ1（ピクセル）:
  [256, 128, 64, 32, 16...] → エッジの検出

フェーズ2（特徴）:
  [エッジ] → [形状：丸い, 耳：三角形] → 猫らしさの特徴

フェーズ3（概念）:
  [特徴] → [動物, 四本足, ひげ] → 「猫」のカテゴリ確定

フェーズ4（推論）:
  確度: 99.2% で「猫」と判定
  根拠: [毛色, 耳の位置, 顔の形状]

【温度とランダム性】

AIの出力には「温度」というパラメータがあります：

  T = 0.1（極度に低い）
    → ほぼ確定的、バラツキなし
    → 分析的・正確な回答

  T = 0.7（バランス）
    → やや創造的、適度なバラツキ
    → 自然な説明

  T = 1.5（高い）
    → 高度に創造的、大きなバラツキ
    → 新しいアイデア生成

【大規模言語モデル（LLM）の場合】

このニューラルネットワークの仕組みを数十億のパラメータで実現したのが
ChatGPTやClaudeなどのLLMです：

  1. トークン化: 文を単語に分割
  2. エンベディング: 単語を数値ベクトルに変換
  3. トランスフォーマー層: 文脈を理解
  4. 注意メカニズム: 重要な単語に焦点
  5. 確率分布: 次の単語の確率を計算
  6. トークン生成: 確率が高い単語を選択

【まとめ】

AIの仕組みの本質は：
  ✓ 単純な数学的操作を何十億回繰り返す
  ✓ 勾配降下法で最適値を探索
  ✓ 膨大なデータパターンから統計的に学習
  ✓ 入力に対して統計的に最もあり得そうな出力を生成

つまり、AIは「魔法」ではなく、数学とデータに基づいた
確率的な予測システムです。

ただし、その予測能力は膨大なデータと計算量により、
人間の直感に近い、時に人間を超える判断ができます。
`, colors.reset);

  await sleep(1000);

  log("\n" + "═".repeat(75), colors.dim);
  log("📊 Processing Statistics\n", colors.yellow);

  log("  • 処理時間: 1,250ms");
  log("  • 生成トークン数: 847");
  log("  • 段落数: 7");
  log("  • 例示数: 15");
  log("  • 数式/コード表現: 12");
  log("  • 階層的説明レベル: 4段階\n");
}

// ============================================================================
// Follow-up Question 1
// ============================================================================

async function followUpQuestion1() {
  section("フォローアップ質問1");

  log("ユーザー質問:\n", colors.cyan);
  log("「では、ニューラルネットワークが実際に学習する過程をもっと詳しく」\n", colors.bright);

  await sleep(800);

  log("═".repeat(75), colors.dim);
  log("🧠 QBNN Response:\n", colors.green);

  log(`素晴らしい質問です。学習の詳細なメカニズムを図解します：

【勾配降下法の視覚化】

損失関数の景観を想像してください：

        Loss（損失）
        │     ⛰
        │    ╱ ╲      ← 初期値（ランダム）
        │   ╱   ╲
        │  ╱     ╲
        │ ╱       ╲     ← 最適値に向かって下る
        │╱_________╲
        └─────────── Weight（重み）

学習プロセス：

  Iteration 1:
    位置: Weight = 3.5, Loss = 2.4
    勾配: ∂L/∂w = 0.8（下り坂の傾き）
    更新: w_new = 3.5 - 0.01 × 0.8 = 3.492

  Iteration 2:
    位置: Weight = 3.492, Loss = 2.38
    勾配: ∂L/∂w = 0.75
    更新: w_new = 3.492 - 0.01 × 0.75 = 3.485

  Iteration 3:
    位置: Weight = 3.485, Loss = 2.37
    勾配: ∂L/∂w = 0.71
    更新: w_new = 3.485 - 0.01 × 0.71 = 3.478

  ... （数万回の繰り返し）

  最終的に:
    位置: Weight = 2.0, Loss = 0.05 ← 最適値に接近

【バッチ学習とミニバッチ学習】

大規模データセットの場合、全データで一度に学習するのは効率が悪いので：

  バッチサイズ = 32のミニバッチ学習：

    Epoch 1:
      Batch 1 (サンプル 1-32): Loss下降
      Batch 2 (サンプル 33-64): Loss下降
      Batch 3 (サンプル 65-96): Loss下降
      ...
      全Batch処理後: Epoch完了

    Epoch 2:
      データをシャッフルして再び学習
      さらにLossが低下

    Epoch 3:
      同様に繰り返し
      LossはほぼプラトーA（飽和）に達する

このプロセスを「エポック」という単位で複数回（通常は10-100回）
繰り返します。

【過学習と正則化】

注意すべき現象：

  ┌─ Training Loss（訓練データの誤差）
  │    ╲
  │     ╲___   ← 最適点
  │         ╲___
  │
  └─ Validation Loss（検証データの誤差）
      ╲
       ╲____
            ╲╲╲  ← 過学習の開始（訓練データに過度に特化）

正則化技術で対策：
  • L1/L2正則化: 大きすぎる重みにペナルティ
  • ドロップアウト: ランダムにニューロンを無効化
  • 早期停止: 検証精度が下がり始めたら学習終了

【実装レベルの詳細】

PyTorchやTensorFlowでの実装例：

  for epoch in range(num_epochs):
    for batch in dataloader:
      # 1. Forward pass
      predictions = model(batch.input)

      # 2. 損失計算
      loss = criterion(predictions, batch.target)

      # 3. Backward pass（自動微分）
      optimizer.zero_grad()
      loss.backward()  ← 自動的に全ニューロンの勾配を計算

      # 4. 重みの更新
      optimizer.step()  ← Adam、SGD等のオプティマイザが更新

自動微分により、開発者は手作業で勾配を計算する必要がありません。

【学習曲線の解釈】

典型的な学習曲線：

  Loss
  │
  1.0 ●●●●●●    ← 初期（ランダムに近い予測）
      │ ╲
  0.5 │  ╲●●●●  ← 急速な改善
      │    ╲
  0.1 │     ╲●●  ← 徐々に減速
      │      ╲
  0.01│       ●  ← 収束（効果的に改善できず）
      └──────────
        0   100  200  300
        Epoch（学習回数）

良い学習：
  ✓ 損失が単調に減少
  ✓ 訓練と検証の曲線がほぼ追従
  ✓ 過度な振動がない

悪い学習兆候：
  ✗ 損失が振動している → 学習率が高すぎる
  ✗ 訓練損失は減るが検証損失は増加 → 過学習
  ✗ 損失が全く減らない → 学習率が低すぎるか，モデルが不適切

【バックプロパゲーションの数学的詳細】

連鎖律を使って逆方向に勾配を伝播：

  Output = f(g(h(Input)))

  dOutput/dInput = dOutput/d(f_out) × d(f_out)/d(g_out) × d(g_out)/d(h_out) × d(h_out)/dInput

各層で局所的に勾配を計算し、乗算していくことで全体の勾配を効率的に求めます。

このため、100層のニューラルネットワークでも、
フォワードパスと同じ計算量（2倍程度）でバックプロパゲーションができます。
`, colors.reset);

  await sleep(800);

  log("  • 処理時間: 950ms");
  log("  • 生成トークン数: 612");
  log("  • 図解数: 4");
  log("  • 数式: 8");
  log("  • 実装例: 1\n");
}

// ============================================================================
// Follow-up Question 2
// ============================================================================

async function followUpQuestion2() {
  section("フォローアップ質問2");

  log("ユーザー質問:\n", colors.cyan);
  log("「LLMの場合、どうやってテキスト生成をしているのか？」\n", colors.bright);

  await sleep(800);

  log("═".repeat(75), colors.dim);
  log("🧠 QBNN Response:\n", colors.green);

  log(`素晴らしい質問です。LLMのテキスト生成の仕組みを詳しく説明します：

【基本的な流れ】

1. 入力テキストの準備

  入力: "明日の天気は"

  ↓（トークン化）

  トークン列: ["明", "日", "の", "天", "気", "は"]
              [102, 45, 203, 156, 78, 91]  ← トークンID

2. エンベディング（埋め込み）

  各トークンIDを高次元ベクトルに変換

  102 → [0.2, -0.5, 0.8, 0.1, -0.3, 0.6, ...]  ← 768次元（例）
  45  → [0.1, 0.3, -0.2, 0.7, 0.4, -0.1, ...]
  ...

  このベクトルは単語の「意味」を表現しています。
  似た意味の単語は似たベクトルになります。

3. トランスフォーマー層での処理

  自己注意メカニズム（Self-Attention）:

  「明日」という単語が現れた時：
    • 「の」との関係を学習 → 関連度: 0.9
    • 「天気」との関係を学習 → 関連度: 0.95
    • 「は」との関係を学習 → 関連度: 0.8
    • 「明」との関係を学習 → 関連度: 0.7

  こうして文脈情報が集約されます。

  複数の注意ヘッドが並列に異なる関係を学習：
    ヘッド1: 文法的な関係
    ヘッド2: 意味的な関係
    ヘッド3: 文脈的な関係

4. 次の単語の確率予測

  ネットワークの出力層は語彙サイズ（例：50,000語）の
  確率分布を生成します：

  P("晴れ" | 入力) = 0.35
  P("曇り" | 入力) = 0.28
  P("雨") = 0.22
  P("雪") = 0.08
  P("雷") = 0.05
  ...

  合計: 1.0

5. 次の単語の選択

  温度パラメータによって選択方法が変わります：

  T = 0.1（確定的）:
    最も確率の高い「晴れ」を確定的に選択

  T = 0.7（バランス）:
    確率分布に従ってランダムに選択
    「晴れ」が35%の確率で選ばれ、
    「曇り」が28%の確率で選ばれる

  T = 1.5（創造的）:
    「雪」や「雷」など、低確率の選択肢も選びやすくなる

6. トークンの追加と再実行

  選ばれたトークン（例："晴れ"）を入力に追加：

  ["明", "日", "の", "天", "気", "は"]
  + ["晴", "れ"]
  =
  ["明", "日", "の", "天", "気", "は", "晴", "れ"]

  このプロセスを繰り返して、テキストを1語ずつ生成します。

【生成例の詳細】

入力: "AIの仕組みは"

ステップ1:
  入力: ["AI", "の", "仕", "組", "み", "は"]
  出力分布:
    P("複雑") = 0.18
    P("ニューラル") = 0.15
    P("機械学習") = 0.12
    ...
  選択: "ニューラル"（確率サンプリング）

ステップ2:
  入力: ["AI", "の", "仕", "組", "み", "は", "ニューラル"]
  出力分布:
    P("ネットワーク") = 0.42 ← 文脈を反映して確率が大幅に上昇
    P("により") = 0.15
    P("な") = 0.08
    ...
  選択: "ネットワーク"

ステップ3:
  入力: [..., "ニューラル", "ネットワーク"]
  出力分布:
    P("に") = 0.38
    P("の") = 0.22
    ...
  選択: "に"

ステップ4:
  入力: [..., "ニューラル", "ネットワーク", "に"]
  出力分布:
    P("よ") = 0.35
    P("基") = 0.18
    ...
  選択: "よ"

... 繰り返し ...

最終出力:
"AIの仕組みはニューラルネットワークに基づいており、
複数の層が相互に作用して学習します。"

【注意機構の詳細】

Transformerの注意メカニズムは次のように動作します：

  クエリ(Q): 「ネットワーク」という単語が何に注意を向けるか
  キー(K): 全てのトークンの「注意対象としての特性」
  バリュー(V): 全てのトークンの「実際の情報」

  注意スコア = softmax(Q · K^T / √d_k)

  出力 = 注意スコア · V

具体例：
  Q（"ネットワーク"の観点）:
    "AI" との関係 → スコア: 0.8
    "の" との関係 → スコア: 0.3
    "仕組み" との関係 → スコア: 0.9
    "は" との関係 → スコア: 0.5

  softmaxで正規化：
    "AI": 0.35
    "の": 0.10
    "仕組み": 0.40
    "は": 0.15

  この重み付けで全トークンの情報を集約します。

【複数ヘッドの並列処理】

例えば12ヘッドの場合：

  ヘッド1: 主語と述語の関係を学習
  ヘッド2: 修飾詞と被修飾語の関係
  ヘッド3: 時間的な前後関係
  ヘッド4: 概念的な関連性
  ヘッド5-12: その他の複雑な関係

  全ヘッドの出力を連結して、次の層に渡します。

【停止条件】

テキスト生成は以下のいずれかで停止します：

  1. 終了トークン（</s>）が生成された
  2. 最大トークン数に達した
  3. ユーザーが生成を停止指示

  終了トークンまでの生成例：
  "AIは..."
  P("</s>") = 0.05（低確率で終了トークンを選択）
  → 継続

  ... 何度か繰り返し ...

  最終的に:
  P("</s>") = 0.45（文の終わりで確率が上昇）
  → 終了トークン選択 → 生成完了

このメカニズムにより、自然で論理的なテキストが生成されます。
`, colors.reset);

  await sleep(800);

  log("  • 処理時間: 1,100ms");
  log("  • 生成トークン数: 728");
  log("  • 図解数: 5");
  log("  • 具体例: 3");
  log("  • 数式: 4\n");
}

// ============================================================================
// Session Summary
// ============================================================================

function sessionSummary() {
  section("Chat Session Summary");

  log("セッション統計\n", colors.bright);

  log("質問数: 3");
  log("総応答トークン数: 2,187");
  log("総処理時間: 4,100ms（約4秒）");
  log("平均応答時間: 1,367ms/質問\n");

  log("トピックカバレッジ\n", colors.bright);
  log("  ✓ ニューラルネットワークの基本構造");
  log("  ✓ 層状処理と情報フロー");
  log("  ✓ 勾配降下法と誤差逆伝播");
  log("  ✓ 双方向処理（QBNN概念）");
  log("  ✓ 大規模言語モデルの仕組み");
  log("  ✓ 自己注意メカニズム");
  log("  ✓ トークン化とエンベディング");
  log("  ✓ テキスト生成の詳細プロセス");
  log("  ✓ 温度パラメータの役割");
  log("  ✓ 学習曲線の解釈\n");

  log("QBNNの強み\n", colors.bright);
  log("  • 複雑なトピックを階層的に説明");
  log("  • 数学的正確性を保ちながら直感的に描写");
  log("  • 具体例と抽象概念の両方を提供");
  log("  • 図解を活用した視覚的理解");
  log("  • 実装レベルの詳細も含む\n");

  log("このセッションについて\n", colors.bright);
  log("  このデモは、QBNNのシミュレーション応答を示しています。");
  log("  実際のQBNNモデルは、HuggingFaceのAPIを通じて");
  log("  より多様で動的な応答を生成できます。\n");
}

// ============================================================================
// Main
// ============================================================================

async function main() {
  console.clear();

  log("╔═══════════════════════════════════════════════════════════════════╗", colors.bright);
  log("║                                                                   ║", colors.bright);
  log("║       🤖 QBNN Chat Session: How AI Works (AIの仕組み) 🤖        ║", colors.bright);
  log("║                                                                   ║", colors.bright);
  log("║  Quantum-inspired Bidirectional Neural Network                   ║", colors.bright);
  log("║  Interactive Educational Chat Demonstration                     ║", colors.bright);
  log("║                                                                   ║", colors.bright);
  log("╚═══════════════════════════════════════════════════════════════════╝", colors.bright);

  try {
    await qbnnChatSession();
    await sleep(1000);

    await followUpQuestion1();
    await sleep(1000);

    await followUpQuestion2();
    await sleep(1000);

    sessionSummary();

    log("═".repeat(75), colors.bright);
    log("\n🎓 Educational Chat Complete!\n", colors.green);

    log("このQBNNチャットセッションのポイント：\n", colors.bright);
    log("  1. 複数のレベルで同じトピックを解説");
    log("  2. 理論と実装の両面からのアプローチ");
    log("  3. 具体例を用いた具体化");
    log("  4. 段階的な深化");
    log("  5. インタラクティブな学習体験\n");

    log("実際に試す：\n", colors.cyan);
    log("  npm start -- --model qbnn\n");
    log("  （ただし、HuggingFace APIトークンが必要です）\n");
  } catch (error) {
    console.error("Error:", error);
    process.exit(1);
  }
}

main();
