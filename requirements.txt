torch>=2.0.0
numpy>=1.24.0
sentencepiece>=0.1.99
gradio>=5.0.0
datasets>=2.18.0
# The managed inference container (inference-pytorch-cpu:sha-2d4f7f1) ships
# huggingface-hub==1.6.0, so we need transformers>=4.53.0 which supports
# huggingface-hub>=1.0.  This resolves the ImportError that previously
# crashed the endpoint at startup.
transformers>=4.53.0
huggingface_hub>=1.0.0,<2.0.0
